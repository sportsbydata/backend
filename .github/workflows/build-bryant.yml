name: build-bryant

on:
  workflow_dispatch:

permissions:
  contents: read
  packages: write
  id-token: write

jobs:
  build-push:
    uses: ./.github/workflows/build.yml
    with:
      binary: bryant
